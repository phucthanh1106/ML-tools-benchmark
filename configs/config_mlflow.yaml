# Configuration file for unified training runs
experiment: "YOLOv12_Detection"
task: "detection"
architecture: "yolo12m"          # Base architecture scale tag

# Path to dataset and weights
weights: "/home/ubuntu/workspace/thanhpt/benchmark/weights/best.pt"

hyperparams:
  data:   # Dynamically resolved via Python script/environment variables
  epochs: 5           # Can be overridden via script arguments (e.g., args.epochs)
  imgsz: 640
  batch: 8              # Can be overridden via script arguments (e.g., args.batch)
  workers: 4
  patience: 10
  save: true
  pretrained: true
  optimizer: "AdamW"
  lr0: 0.001
  lrf: 0.001
  momentum: 0.937
  weight_decay: 0.0005
  warmup_epochs: 3
  mixup: 0.3
  mosaic: 0.0
  name: "Full"
  exist_ok: false
  device: null          # Dynamically resolved via torch.backends check in python engine