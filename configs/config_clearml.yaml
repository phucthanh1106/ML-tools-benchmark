# Configuration file for unified training runs
experiment: "YOLOv12_Detection"
task: "detection"
architecture: "yolo12m"          # Base architecture scale tag

# Path to dataset and weights
weights: "/home/ubuntu/workspace/thanhpt/ML-tools-benchmark/weights/best.pt"

data_id: "450ffa4202694729abe163c2b843fef7"

hyperparams:
  data:   # Dynamically resolved via Python script/environment variables
  epochs: 5           # Can be overridden via script arguments (e.g., args.epochs)
  imgsz: 640
  batch: 32              # Can be overridden via script arguments (e.g., args.batch)
  workers: 64
  patience: 10
  save: true
  pretrained: true
  optimizer: "AdamW"
  lr0: 0.001
  lrf: 0.001
  momentum: 0.937
  weight_decay: 0.0005
  warmup_epochs: 3
  mixup: 0.3
  mosaic: 0.0
  name: "Full"
  exist_ok: false
  device: [0, 1]        # Dynamically resolved via torch.backends check in python engine